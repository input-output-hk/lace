import { it, expect } from 'vitest';

it.todo(
  'add tests; this was added as minimal package setup with npm scripts and test runner configured',
);

it('should pass', () => {
  expect(true).toBe(true);
});
