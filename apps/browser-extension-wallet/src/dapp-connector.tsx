import * as React from 'react';
import { render } from 'react-dom';
import 'antd/dist/antd.css';

const mountNode = document.querySelector('#lace-popup');
render(<div>dapp connector</div>, mountNode);
