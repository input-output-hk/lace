# @lace-module/storage-extension

Promise-based WebExtension/BrowserExt API
https://github.com/mozilla/webextension-polyfill

Uses `@lace-contract/storage` store classes by passing the custom storage-api-adapter which abstracts
over the web extension API. Provides the store dependencies required to fulfill the storage contract.
