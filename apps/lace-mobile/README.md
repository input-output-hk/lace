# Lace Mobile

## Building with Expo Plugin

To generate native iOS and Android projects:

1. Navigate to the `lace-mobile` directory:

   ```sh
   cd apps/lace-mobile
   ```

2. Run Expo prebuild:

   ```sh
   npx expo prebuild --clean
   ```

The Expo build plugin runs automatically during the prebuild process.
