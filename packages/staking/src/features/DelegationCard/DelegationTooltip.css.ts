import { sx } from 'features/theme';

export const tooltip = sx({
  background: '$tooltipBgColor',
  borderRadius: '$small',
  boxShadow: '$tooltip',
  margin: '$10',
  maxWidth: '$214',
  padding: '$16',
});
