import * as React from 'react';
import { createRoot } from 'react-dom/client';
import 'antd/dist/antd.css';

const mountNode = document.querySelector('#lace-popup');
createRoot(mountNode).render(<div>dapp connector</div>);
