{
  "projectType": "library",
  "tags": ["scope:lib"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "node scripts/build.mjs",
        "cwd": "{projectRoot}"
      }
    },
    "lint": {},
    "type-check-src": {}
  }
}
