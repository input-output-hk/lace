# LMP Bundle

Merges Lace v1 extension with LMP build into a unified browser extension.

## Build Instructions

1. Place LMP build at `apps/lmp-bundle/lmp-build/dist/`
2. Run from repository root:
   ```sh
   yarn build:bundle:full
   ```
3. Output: `apps/lmp-bundle/dist/`
