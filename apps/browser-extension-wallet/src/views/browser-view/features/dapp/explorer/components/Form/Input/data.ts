/* istanbul ignore next */
export const initRegister = () => ({
  onChange: async () => {
    // eslint-disable-next-line no-console
    console.log(test);
  },
  onBlur: async () => {
    // eslint-disable-next-line no-console
    console.log(test);
  },
  ref: () => ({}),
  name: undefined,
  min: 0,
  max: 0,
  maxLength: 0,
  minLength: 0,
  pattern: '',
  required: false,
  disabled: false
});
