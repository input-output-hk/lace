import { fonts, colors, opacity, shadow, border, grid, padding, container, mixins } from './Themes';

const Theme = {
  fonts,
  colors,
  border,
  shadow,
  opacity,
  grid,
  padding,
  container,
  mixins
};

export { Theme };
